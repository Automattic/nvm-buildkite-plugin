#!/usr/bin/env bash

set -euo pipefail

if [[ -z "${NVM_BUILDKITE_PLUGIN_TEST_NVMRC_VERSION:-}" ]]; then
    exit 0
fi

# CI-only fixture for exercising plugin fallback to .nvmrc.
printf '%s\n' "$NVM_BUILDKITE_PLUGIN_TEST_NVMRC_VERSION" > .nvmrc
